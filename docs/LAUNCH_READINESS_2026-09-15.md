# 内容证据与交付复核 2026 年 9 月 15 日

本记录区分维护实现、合成诊断与实际云端交付，不替换 v0.6.0 的历史发布证据。
截至本轮开始，维护源为 `f9bf4c8661eb433e6ae737660fdd8fa58032b5fa`，正式版本仍为 v0.6.0。

## 建议取舍

| 建议 | 决定与边界 |
| --- | --- |
| 正文声明与引用片段对应 | 实施独立诊断，补齐缺引用、表格及长句；词面匹配不等于事实证明，不直接升级为未经校准的审批硬门禁。 |
| 独立内容评测 | 使用实现前另行编写并冻结的 24 例合成挑战；与旧六表单、十二目标分开，不伪称真人或独立领域审核。 |
| 新版交付验收与样例 | 先验收后发布；登录下载、Word 全页检查等未完成时，不发新正式版本或替换旧样例。 |
| 失败修订恢复与进度 | 保留可编辑的临时请求及真实失败原因；保持原报告、质量结果和审计事务不变。 |
| README 首屏 | 压缩重复维护历史，提前展示用途、演示、样例、运行入口与限制。 |
| 真实使用反馈 | 仍需真实参与者；合成测试不能替代可用性反馈或领域事实审核，不填造参与者和结果。 |

## 已核实的默认启动恢复

只读检查 Railway production 服务配置及一次新提交部署的启动日志，补齐
[9 月 12 日记录](LAUNCH_READINESS_2026-09-12.md)中临时观察参数移除的待验收项。

- 服务启动命令：`python scripts/start_container.py`。
- 新部署：`e9b741ad-8595-43dd-83ef-de55237876e9`，状态 `SUCCESS`。
- 部署源：`f9bf4c8661eb433e6ae737660fdd8fa58032b5fa`。
- 检查范围：2026-09-12 06:28:50–06:30:00 UTC 的该部署启动日志。
- `2026-09-12T06:29:31.333758050Z`：`container_ready=true`，私有 RAG 就绪。
- `2026-09-12T06:29:31.333765590Z`：`model_usage`、`phase=startup`、
  `schema_version=1`、`day=2026-09-12`、`status=ready`、`calls=0`。
- 该启动日志范围没有额外的 2026-09-11 历史观察记录；配置也不再携带 `--observe-usage-day`。

这证明新部署应用了正常启动配置，不把当前日零计数当作历史配额恢复证据。
历史日非零配额恢复仍引用 9 月 12 日记录中两次不同部署的独立读数。
本次检查没有读取密钥、访问口令或其他用户报告。

## 独立合成挑战的冻结边界

数据文件：`data_australia/rag/content_holdout_v1.json`。
冻结 SHA-256：`4cc42051ee29babc6fd18903e225a691dd5a97f1de448261681cd43187c9dcc2`。
由独立任务在新正文诊断实现前编写；实现者在首次评测前不读取题目或标签。
24 例覆盖否定、条件与例外、数字口径、未知、引用完整性、格式等价性，
其中含表格与超过 600 字符的长句。不得为提高通过率回改标签。

这是合成挑战集，不是来自真实用户的盲测，也不能据此声称语义蕴含正确率或领域事实准确率。
首次运行后若据此修复实现，后续结果将标为针对已见挑战的回归，不继续称为未见评测。

### 首次测量及失败保留

[首次适配失败记录](diagnostics/content-holdout-first-2026-09-15.json)绑定正文诊断源文件
`089d726ec6be913aa339f72f51b29d490b1767e62cdc4546b711075ae515ef16`。
24 例仅 1 例完成，23 例因合成来源标签与应用引用格式没有正确转换而被报告为抽取缺失。
这是评测适配失败，不能当作内容准确率；原文件保留，未用后来的成功结果覆盖。

随后只转换合成引用注记、不修改自然语言正文及预期标签，并纳入独立代码复核发现的
缩进代码围栏排除修复。其[修正适配后的记录](diagnostics/content-holdout-adapter-corrected-2026-09-15.json)
绑定正文源文件 `49eee6523d93b02c25b2913c813994bb64a30b4f56b1565f67331610a4c901d7`：

- 24 例中 23 例完成，1 例是预先排好 Markdown 表格被适配器再次嵌套的问题。
- 已完成的 23 例：11 个应复核案例被标记，1 个漏判；8 个无须复核案例未标记，3 个误报。
- 漏判是同一百分比跨家庭与企业、不同统计指标混用，说明整段词汇匹配的边界。
- p50 0.958 ms、p95 1.699 ms 为该次合成捕获加正文诊断耗时，不是检索或模型响应时延；
  该测量未执行任何真实模型、检索或嵌入调用。

两份记录的 SHA-256 分别为
`82ccf2299d7e777b8d635df1f1e758d7bd2b887edb889f810c5f592736b6e680` 和
`7b3d76390afb71ae1c7aaf06d0951d9f7ec2b012a4e0fbd29d0ebba2f549eef9`。
它们在工作树中运行并明确记录 dirty 状态，不冒充干净提交的正式发布证据。
后续针对数字口径和完整表格适配的修正，将另存为已见案例回归。

### 已见案例回归与保留的限制

[后续回归记录](diagnostics/content-seen-regression-2026-09-15.json)明确标记
`evaluation_phase=seen_case_regression`。修复仅涉及完整表格的适配与逐单元格覆盖检查，
以及重复数值跨不同来源语句混用时的保守复核提示，没有改写冻结题目、预期标签或前两次记录。

24 例均完成、抽取遗漏 0；12 个应复核案例被标记、漏判 0，8 个无须复核案例未标记、误报 4。
误报涉及限定条件的释义、未知信息的不同表达，以及表格中的未引用地点标签。
它们作为已知限制保留，不通过案例 ID 分支或修改预期标签来消除。
这不是未见案例成绩，也不能写作事实准确率、真人验证或语义理解通过率。

正文诊断源文件 SHA-256：`bf06e3a453b0fb05c779fd268fdb4e34a8511bca33bdf8aebb0e1ad4c2364731`；
回归文件 SHA-256：`c933340a024b2457da95ad909325d6bd4300ff8be14404b32f5cabe38f7d97e1`。
该次合成捕获与诊断的 p50/p95 为 1.204/1.838 ms，真实模型、检索、嵌入调用均为 0。
它不能与真实模型端到端时延混为一谈。六表单检索嵌入 6→25 的效果与开销比较仍见
[原成对 CPU 诊断](diagnostics/form-context-comparison-ac70710-2026-09-12.json)，本轮未改动检索融合逻辑。

## 已实施的功能与兼容边界

- 新增 `body_claim_evidence_v1`：区分缺引用、无词面支持、证据未知和无须外部引用；
  保留完整长句、列表、表格单元格及原文位置，排除隐藏内容与代码。
- 片段来自最终实际 SDK 提交捕获，引用片段通过来源、块标识、位置及哈希校验。
  引用缺失不再因缺少 SDK 捕获而变成无须评估；未知与零支持分别显示。
- 初次生成、结构修复及修订增加正文引用指导。初版曾以零正文引用触发额外重写；
  本轮真实回归发现该策略无效且可能引入安全问题，后续撤回这一单独触发条件，详见下文。
  新诊断仍不改变 `governed-report-v6`，旧全来源 v4、可见来源 v1 与历史样例不回填。
- 界面提供完整声明、对应已提交片段及待复核原因；新导出包重新计算并验证正文诊断，
  当前界面诊断不覆盖历史审计结果。
- 失败修订请求仅暂存在当前应用会话，绑定原报告与审计版本，可编辑、重试或丢弃；
  失败候选不替换原报告和原质量结果。最终写入失败不能自动再次调用模型。
- 进度展示来自真实阶段、尝试类型和计时，不显示虚构完成百分比；
  外部模型隐私说明补充修订时实际提交的冻结 RAG 片段。

## 本地验证

Windows Python 3.13.9 非 E2E 全套：`2214 passed, 7 skipped, 6 deselected`，覆盖率 89.58%，
耗时 186.88 秒。全套采集之后新增的空白修订反馈回归，连同恢复与进度界面测试共 7 项通过；
进度回调异常隔离的等价改动另有 9 项针对性测试通过。
Ruff 检查及全库格式检查、Bandit、依赖锁一致性、`pip check`、已知依赖漏洞检查通过。
独立只读代码复核发现的抽取边界与空白重试反馈问题均已修复并复核关闭。

Chromium 浏览器 E2E：6 项通过，耗时 55.83 秒。
在干净维护提交 `0378229a4cd5591d0d87c0708269a1a1dd8fcfb6` 上，
v0.5.0 与 v0.6.0 的历史发布证据及文件包离线验证均通过。
[跨平台 Tests CI](https://github.com/shuxiachai/BushfireReadyGPT/actions/runs/34974882663)
与 [Docker smoke CI](https://github.com/shuxiachai/BushfireReadyGPT/actions/runs/34974882650) 均成功；
这不替代真实模型、云端下载或 Word 全页验收。

## 首次真实模型回归：未通过

2026-09-15 13:20:47–13:27:27 UTC，在上述干净提交运行完整八场景 Ollama 回归。
运行前后源、模型及索引身份稳定；使用现有 `bushfire-ready-qwen`，没有下载或更换模型。
本地结果保留于 `output/diagnostics/report-body-claims-0378229-2026-09-15.json`，
SHA-256 为 `d3ba30a188db2721a66ab731cc204b58ac2a07f432e7c69b33e260289d4ccb16`。
该文件不覆盖旧正式样例，本节仅公开必要的汇总结果。

- 完整发布回归门禁未通过：7/8 份报告通过既有治理门禁，WA 农场场景在三次尝试后仍触发
  `absolute_safety_guarantee`。安全门禁阻止了该报告通过，不放宽规则消除失败。
- 六个检索场景中，五个最终仍无正文引用；VIC 养老照护场景有 7 个带引用声明，
  但诊断均未发现词面支持。不能把“出现引用”或来源区有标题当作结论有依据。
- 两个无检索场景不要求交付 RAG 正文引用，不把它们计作引用成功案例。
- 结构、证据绑定、来源标题归属、检索行为及场景主题各项通过率均为 100%；
  这些通过项不能抵消正文内容与安全方面的失败。
- 平均端到端时延 49.85 秒，p95 71.39 秒；6/8 场景发生修复。
  这是本机本次实测，不与不同模型、硬件或检索配置的旧均值直接比较。

这次失败说明：新增可观测性和修复触发器并不等于模型已能稳定交付有依据的正文。
后续修复和复测必须另存结果，不将这次记录改写成通过。

### 提示修正后的有限复测与策略收缩

在工作树中补充完整引用格式、明确修复原因，并将初次生成和修复的叙事目标统一为
650–800 词后，另行复测 QLD 学校与 WA 农场。保持同一模型与 CPU 索引，
2300 输出 token、至少 300 词正文、15 个固定章节、两次修复上限和治理 v6 均未放宽。

| 有限复测 | 最终治理门禁 | 尝试次数 | 待引用声明 / 已引用声明 | 时延 |
| --- | --- | ---: | ---: | ---: |
| QLD 学校 | 通过 | 3 | 50 / 0 | 74.70 秒 |
| WA 农场 | 通过 | 3 | 47 / 0 | 67.79 秒 |

这是两次已见案例诊断，不是干净提交的完整发布回归；发布门禁未激活。
两次有限运行的修复率都高于套件阈值，CLI 均返回 1。两份报告的正文引用交付仍为失败；
治理门禁通过不能解释为具体结论已有支持。文件分别保留为
`output/diagnostics/qld_school_cairns-postfix-diagnostic-2026-09-15.json` 与
`output/diagnostics/wa_farm_margaret_river-postfix-diagnostic-2026-09-15.json`，SHA-256 分别为
`31b7d226688c0ea8a91da69eff111967c79f9c7fd52e3994097da5ce6cfb22d6` 和
`c4770dd811a94b4a13a1af4a177a82390147baa1633ec560beff97d2bc4e3aa6`。

由未改动代码的私有逐次捕获复核可见，QLD 与 WA 初次候选均已通过既有治理门禁，
后续仅为缺引用而增加的重写反而产生了安全保证措辞。独立复核因此建议撤回
**仅由建议性缺引用状态触发的额外重写**，保留原有必需的安全及结构修复。
缺引用仍明确显示，正文交付指标仍为失败，不靠删除诊断或降低门禁宣布解决。
必需修复可以同时收到引用指导，但引用指导不独立增加调用次数。

同时修正两处确定性判断：精确、由规范场景与关注点 ID 生成的应用范围声明保留可见，
但不计为外部事实；用户背景中的引用不再满足外部声明的引用交付。
新的 summary v2 只表示“需要证据的声明中存在引用”，仍不表示词面或语义支持。
旧 summary v1 和没有分类范围元数据的旧诊断保留原算法重放，历史记录不回填。

### 最终维护代码的自动验证

撤回建议性重写并修正分类与版本兼容之后，完整非 E2E 回归为
`2229 passed, 7 skipped, 6 deselected`，覆盖率 89.63%，耗时 190.19 秒。
全部 Chromium E2E 为 `6 passed, 2236 deselected`，耗时 45.36 秒。
Ruff、全库格式检查及 Bandit 通过；v0.5.0/v0.6.0 离线验证在明确记录的
`--allow-dirty` 诊断模式下通过，提交后的干净源验证另行确认。
独立只读复核关闭了分类、计数、历史重放及额外重写边界问题。
这些是软件回归结果，不改变上述真实引用交付未通过的结论。

## 维护部署确认

Railway 部署 `f0a57d70-b5c6-4bc7-9968-ce25ea88fb8f` 对应 `0378229`，状态 `SUCCESS`。
2026-09-15 13:28:35 UTC 启动日志显示 `container_ready=true`、私有 RAG `ready`，
当日 SQLite 配额状态 `ready`、`calls=0`。只读核查没有读取密钥或其他用户记录。
这证明维护代码已部署并启动，不代表完整交付验收或新正式版本发布。

### 登录后的首次云端生成：未完成

在上述部署使用已有口令登录后，通过正常表单加载 Cairns Council pilot，
机构和附加背景均明确标为 2026-09-15 合成软件验收，不代表真实 Council 或专业审核。
首次生成共执行三次模型请求，最终提示 `The model reached its output limit before completing the report.`。
界面显示真实阶段与 39.4 秒总耗时；13:36–13:37 UTC 的部署日志记录三次配额提交，
以及两条 `Model response rejected (length).`，不把所有请求都推断为相同失败。
没有反复点击生成、读取口令、放开鉴权或调整配额绕过失败。

因此该次没有进入修订、审核及下载阶段；登录成功与进度显示不能视为完整交付通过。
后续调整必须保留现有输出上限和安全门禁，并重新执行实际云端流程。

## 交付边界

完整云端文件包仍须经现有口令登录后实际下载，核对报告、版本、审计与导出清单关联。
本轮打包依赖仍为 `26.909.12148`，未提供绑定的 LibreOffice 渲染器；
Word 全页版面验收尚未通过，不以 OOXML 或纯文本检查替代逐页视觉检查。

当前结论仍是受控演示维护中，不是新正式发布、商业上线批准或外部试点通过。
